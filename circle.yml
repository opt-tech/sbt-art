machine:
  timezone: Asia/Tokyo

  java:
    version: oraclejdk8

dependencies:
  cache_directories:
    - "~/.ivy2"
    - "~/.sbt"

test:
  override:
    - sbt clean test

  post:
    - find ~/.ivy2 -name "ivydata-*.properties" -delete
    - find ~/.sbt -name "*.lock" -delete
